<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#ADD8E6"
    tools:context=".ScheduleFragment">

    <!-- 상단 날짜 -->
    <TextView
        android:id="@+id/tvCurrentDate"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="날짜 표시"
        android:textSize="18sp"
        android:textStyle="bold"
        android:layout_margin="16dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"/>

    <!-- 캘린더 접기/펼치기 버튼 -->
    <ImageButton
        android:id="@+id/btnToggleCalendar"
        android:layout_width="35dp"
        android:layout_height="35dp"
        android:layout_marginTop="4dp"
        android:layout_marginEnd="4dp"
        android:background="#ADD8E6"
        android:contentDescription="캘린더 접기/펼치기"
        android:padding="8dp"
        android:scaleType="fitCenter"
        android:src="@drawable/ic_arrow"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- 캘린더 컨테이너 -->
    <FrameLayout
        android:id="@+id/calendarContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/tvCurrentDate"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- 주간 캘린더 (먼저 배치 - 위에 보임) -->
        <LinearLayout
            android:id="@+id/weekCalendarLayout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center"
            android:background="#ADD8E6"
            android:layout_marginTop="4dp">

            <ImageButton
                android:id="@+id/btnPrevWeek"
                android:layout_width="20dp"
                android:layout_height="25dp"
                android:src="@drawable/ic_arrow_left"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="이전 주"
                android:scaleType="fitCenter"/>

            <LinearLayout
                android:id="@+id/weekDateContainer"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:orientation="horizontal"
                android:gravity="center" />

            <ImageButton
                android:id="@+id/btnNextWeek"
                android:layout_width="20dp"
                android:layout_height="25dp"
                android:src="@drawable/ic_arrow_right"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="다음 주"
                android:scaleType="fitCenter" />
        </LinearLayout>

        <!-- 월간 캘린더 (뒤에 배치 - 기본은 숨김) -->
        <CalendarView
            android:id="@+id/calendarView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:visibility="visible" />
    </FrameLayout>

    <!-- 일정 리스트 -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvTodoList"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginTop="8dp"
        android:background="#FFFFFF"
        app:layout_constraintTop_toBottomOf="@id/calendarContainer"
        app:layout_constraintBottom_toBottomOf="parent" />

    <!-- 일정 없을 때 안내 -->
    <TextView
        android:id="@+id/tvEmpty"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:background="#FFFFFF"
        android:gravity="center"
        android:text="새로운 일정을 추가해보세요"
        android:textColor="#888888"
        android:textSize="18sp"
        android:visibility="gone"
        app:layout_constraintTop_toBottomOf="@id/calendarContainer"
        app:layout_constraintBottom_toBottomOf="parent"/>



    <!-- 일정 추가 버튼 <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fabAddTodo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:backgroundTint="#ADD8E6"
        android:src="@drawable/ic_add"
        app:layout_constraintBottom_toTopOf="@id/bottomButtonLayout"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginEnd="24dp"
        android:layout_marginBottom="16dp"/>

    <LinearLayout
        android:id="@+id/addOptionBox"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:background="#ADD8E6"
        android:padding="8dp"
        android:layout_marginBottom="10dp"
        android:visibility="gone"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toTopOf="@id/fabAddTodo"
        android:layout_marginEnd="24dp"
        android:elevation="6dp">

        <Button
            android:id="@+id/btnAddGeneral"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="일반 일정"
            android:backgroundTint="#ADD8E6"/>

        <Button
            android:id="@+id/btnAddDday"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="D-Day"
            android:backgroundTint="#ADD8E6"/>

        <Button
            android:id="@+id/btnAddRoutine"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="루틴"
            android:backgroundTint="#ADD8E6" />
    </LinearLayout> -->



</androidx.constraintlayout.widget.ConstraintLayout>